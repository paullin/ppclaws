# 标签分类

## 背景

我需要根据本文档对 OpenClaw 相关的搜索关键词的意图分类标签，对每个关键词的意图进行打标签。

## 分类

- 概念了解：用户搜索某个关键词是为了了解这是什么。例如：'what is openclaw', '龙虾'
- 相关信息：用户搜索某个关键词是为了了解相关信息。例如：'openclaw pricing', 'is openclaw free', 'openclaw china'、'openclaw 原理'
- 周边项目：用户搜索某个关键词为了了解与 OpenClaw 相关的项目。例如：'openclaw rl'、'claw-code'、'clawhub'
- 安装部署：用户搜索某个关键词是为了了解如何安装 OpenClaw。例如：'openclaw 配置'、'openclaw 部署'、'openclaw installation'、'openclaw 更新'
- 渠道接入：用户搜索某个关键词是为了了解如何接入 OpenClaw。例如：'openclaw 接入飞书', 'openclaw telegram', 'openclaw slack'
- 模型配置：用户搜索某个关键词是为了了解如何将 OpenClaw 与大模型进行配置。例如：'openclaw 更换 模型'、'openclaw minimax'
- 技能扩展：用户搜索某个关键词是为了了解如何为 OpenClaw 添加技能。例如：'openclaw skills hub'、'openclaw skill'、'clawhub'
- 使用教程：用户搜索某个关键词是为了了解如何用好 OpenClaw。例如：'How to use openclaw', 'openclaw mcp'、'openclaw documentation'、'openclaw101'、'openclaw agent'、''openclaw mission control''
- 竞品对比：用户搜索某个关键词是为了对比 OpenClaw 与其他竞品。例如：'openclaw alternatives'、'claude dispatch vs openclaw'、'workbuddy'
- 其他未知：你无法准确判断的标签

## 任务

请根据这个分类标签，对每个关键词的意图进行打标签。

- 打标文件：`/Users/paullin/Dropbox/Study/Project/Python/ppclaws/doc/openclaw_google_trend.xlsx`
- 打标对象：文档中的「query」列
- 打标标准：本文档第二部分的分类标签。不得超出这些标签范围
- 输出位置：`/Users/paullin/Dropbox/Study/Project/Python/ppclaws/doc/openclaw_google_trend.xlsx` 的「意图分类-调整」列表
