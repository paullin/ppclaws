# 迭代划分

## 背景

我已经在 [子目录及详情页生成方案](../tech/子目录及详情页生成方案.md) 中撰写了一个子目录及详情页生成方案。

## 任务

接下来，我需要你根据这个生成方案，划分为多个小的迭代任务。便于开发和验收。

## 要求

每个迭代的说明包括但不限于：

- 本次迭代的目标：一句话描述
- 本次迭代的改动范围：改动哪些？细化到文件的增/改/删
- 本次迭代的验收方式：如何验证本次迭代的改动是正确的。
- 输出到 [tech](../tech/)目录下，文档名称为 `子目录及详情页生成迭代计划.md`
