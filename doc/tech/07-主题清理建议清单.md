# PPClaws 网站清理与优化建议清单

在准备以上线域名 `ppclaws.com` 对外发布之前，为了确保项目代码与页面不残留任何与原 `astro-i18n-starter` 主题相关的品牌、链接及无用配置，根据全量扫描的结果，整理出以下清理与修改建议清单：

## 1. 基础配置与文档 (建议修改)

| 文件路径 | 残留内容 | 建议操作 | 具体修改方案 |
| :--- | :--- | :--- | :--- |
| `package.json` | 包含原主题的 `name`, `description`, `author`, `homepage`, `repository` 等信息。 | **修改** | 将 `name` 改为 `ppclaws`，更新 `description`，移除或修改 `author`, `homepage` 和 `repository` 指向你自己的信息。 |
| `README.md` | 完整的旧主题说明文档（包括 Demo 链接、特性等）。 | **修改** | 清空原内容，替换为 PPClaws 项目专属的说明文档（包含如何运行、核心功能等）。 |
| `LICENSE` | `Copyright (c) 2025 Nik Klemenc` | **修改** | 修改为 `Copyright (c) 2025 PPClaws`。 |
| `astro.config.mjs` | `site: process.env.PRODUCTION_DOMAIN \|\| "http://localhost:4321"` 以及定制的 sitemap 中的 `/zh/` 优先级配置。 | **修改/确认** | 确认逻辑是否满足需求，同时确保线上环境变量 `PRODUCTION_DOMAIN` 被正确设置为 `https://ppclaws.com`。 |

## 2. 环境变量与无用配置 (建议删除)

| 文件路径 | 残留内容 | 建议操作 | 具体修改方案 |
| :--- | :--- | :--- | :--- |
| `.env.example` | 残留了 Giscus（评论系统）的大量配置如 `PUBLIC_GISCUS_REPO` 等。 | **删除** | 经过扫描，当前代码中并未使用 Giscus，建议直接删除这些无用环境变量，保持配置整洁。并更新 `PRODUCTION_DOMAIN="https://ppclaws.com"`。 |

## 3. 多语言配置与文案 (建议修改/删除)

| 文件路径 | 残留内容 | 建议操作 | 具体修改方案 |
| :--- | :--- | :--- | :--- |
| `src/locales/en/main.json` | Title、Description、Keywords 和针对旧主题的特性介绍（`intro`, `features` 等）。 | **修改** | 清理掉关于 "Astro i18n Starter Project" 的所有描述，替换为 PPClaws 英文版的品牌标语、SEO TDK 和核心特性。 |
| `src/locales/zh/main.json` | Title、Description、Keywords 和针对旧主题的特性介绍（`intro`, `features` 等）。 | **修改** | 清理掉关于 "Astro i18n 中文示例项目" 的所有描述，替换为 PPClaws 中文版的品牌标语、SEO TDK 和核心特性。 |
| `src/locales/en/common.json` | 菜单 (`menu.list`) 中残留的无用键值：`about`, `pages`, `dynamicRouting`, `subpage-1`, `subpage-2`, `blog`, `pagination`, `contact` 等。 | **删除** | 从 JSON 对象中删除这些无用的菜单键值。 |
| `src/locales/zh/common.json` | 菜单 (`menu.list`) 中残留的无用键值：`about`, `pages`, `dynamicRouting`, `subpage-1`, `subpage-2`, `blog`, `pagination`, `contact` 等。 | **删除** | 从 JSON 对象中删除这些无用的菜单键值。 |

## 4. 代码注释与逻辑 (建议修改)

| 文件路径 | 残留内容 | 建议操作 | 具体修改方案 |
| :--- | :--- | :--- | :--- |
| `src/i18n/routes.ts` | 顶部注释保留了针对斯洛文尼亚语 (`Slovenian`) 的映射说明。 | **修改** | 移除或修改为符合当前项目（中英文）的注释说明。 |
| `src/i18n/utils.ts` | 顶部注释及代码注释内有 `o-projektu` 和 `varnostni-trendi` 的旧主题例子。 | **修改** | 移除旧主题示例注释，替换为当前业务相关的示例或直接精简注释。 |
| `src/layouts/Base.astro` | Open Graph 的默认图片属性指向了 `/og-image.jpg`，但项目 public 目录下不存在该文件。 | **修改** | 修改默认 fallback 路径，并在 `public/` 目录下补充一个 PPClaws 专属的社交分享预览图 (例如 `/social/og-image.png`)。 |

## 5. 图片与图标资源 (待确认/建议修改)

| 文件/目录路径 | 残留内容 | 建议操作 | 具体修改方案 |
| :--- | :--- | :--- | :--- |
| `public/favicon/` | `favicon.ico`, `apple-touch-icon.png` 以及 `site.webmanifest` 配置文件。 | **待确认** | 请确认这些图标是否已经是 PPClaws 的 Logo？如果还是原主题的图标，请提供新图标进行替换，并更新 `site.webmanifest` 中的 `name` 和 `short_name`（当前可能仍为默认或原主题名）。 |
| `src/assets/logo.webp` | 项目的主 Logo 图片。 | **待确认** | 请确认该图片是否已经是 PPClaws 的真实 Logo。如果不是，请提供新的设计资源进行替换。 |
| `public/social/` | 包含了微信、小红书、即刻、YouTube 等社交图标。 | **正常** | 这些看起来是你自己添加的社交媒体图标，无需处理。 |

---

**执行建议：**
如果你确认了上述清单的内容，我可以立即为你执行第一波自动化清理（包括：修改 `package.json`、清理 `.env.example` 废弃变量、重写 `README.md`、修正 `LICENSE`、清理 `main.json` 和 `common.json` 的无用文案及代码注释）。对于需要你提供新图片（如 `og-image.jpg` 和 Favicon）的部分，我们可以留在最后一步处理。

请告诉我是否现在就帮你自动执行这些代码修改？
